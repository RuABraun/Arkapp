<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colBackground</item>
        <item name="buttonStyle">@style/btnstyle</item>
    </style>

    <style name="btnstyle" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="android:translationZ">0dp</item>
        <item name="android:backgroundTint">#ff1a1a</item>
    </style>

</resources>
